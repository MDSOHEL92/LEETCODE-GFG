class Solution:
    def maxPeopleDefeated(self, p):
        # code here
        count = 0
        i = 1
        
        while p >= i * i:
            p -= i * i
            count += 1
            i += 1
            
        return count
