class Solution:
    def visibleBuildings(self, arr):
        # code here
        if not arr:
            return 0
        count = 1
        maxHeight = arr[0]
        
        for i in range(1, len(arr)):
            if arr[i] >= maxHeight:
                count += 1
                maxHeight = arr[i]
                
        return count
