class Solution {
  public:
    int getMaxVal(vector<int> &arr, int k) {
        // mrmdsohel
        sort(arr.begin(), arr.end(), greater<int>());
        
        int sum = 0;
        for(int i=0; i<k; ++i){
            sum += arr[i];
        }
        return sum;
    }
};
